{
  "name": "week-3-code-challenge",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "json-server --host 0.0.0.0 --port $PORT db.json"
  },
  "author": "",
  "license": "ISC",
  "description": "A simple web application to manage blog posts. Users can view, add, edit, and delete posts. The app uses a mock backend powered by `json-server`.",
  "dependencies": {
    "prompt-sync": "^4.2.0",
    "json-server": "^0.17.4"
  }
}
